void main() {
    graph[2] g;
    g[0 to 2];
    g[0] = g[2] = 1;
    for (int vertice : dfs(g, 0, 1)) {
        << vertice;
    }
}

boolean outra(int outro, boolean outro, graph outro) {
    << outro;
    if (a) << outro; else {}
    return true || false;
}

float main() {
    int a = b = c;
    if (a) {
    } else if (b >= outro != s){
        while(a) {}
        for (int b;a;a) {}
        if (a * 10.4) {}
    }
    for (int a: bfs(a,1,1)) {}
    >> outro[10];
}