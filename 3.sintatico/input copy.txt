boolean outra(int outro, boolean outro, graph outro) {
    << outro;
    if (a) << outro; else {}
}

float main() {
    int a =  b = c;
    if (a) {
    } else if (b >= outro != s){
        while(a) {}
        for (int b;a;a) {}
        if (a) {}
    }
    for (int a:) {}
    >> outro[10];
}