
boolean outra(int a, graph b) {
    return true || false;
}

float main() {
    int a;
    a = 0;
    float[10] b;
    graph g;
    addv(g);
    adda(g, 0, 0);
    outra(a, g);
    if (a && b[1]) {
    } else if (b[0] >= 10.0 != 10) {
        while(a) {

        }
    }
    for (a: bfs(g,0)) {
        >> g[0];
    }
}